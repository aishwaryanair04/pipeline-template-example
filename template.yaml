namespace: myNamespace
name: templateName
version: '1.3.2'
description: template for testing
maintainer: foo@bar.com

config:
    jobs:
        main:
            image: docker.ouroath.com:4443/docker.io/node:18
            steps:
                - printLine: echo "Test for validation of pipeline templates"
