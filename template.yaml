namespace: myNamespace
name: templateName
version: '1.3.2'
description: template for testing
maintainer: foo@bar.com

config:
    jobs:
        main:
            image: node:18
            steps:
                - step-1: echo "Testing pipeline templates"
