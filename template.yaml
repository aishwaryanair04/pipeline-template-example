namespace: myNamespace
name: template_name
version: '1.3.2'
description: template for testing
maintainer: foo@bar.com

config:
    jobs:
        main:
            steps:
              - printLine: echo "Testing template creation V3"
